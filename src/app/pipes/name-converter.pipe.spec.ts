import { NameConverterPipe } from './name-converter.pipe';

describe('NameConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new NameConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
