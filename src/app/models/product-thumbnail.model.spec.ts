import { ProductThumbnail } from './product-thumbnail.model';

describe('ProductThumbnail', () => {
  it('should create an instance', () => {
    expect(new ProductThumbnail()).toBeTruthy();
  });
});
